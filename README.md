
Для установки и продолжения разработки внутри контейнера, следует выполнить следующие шаги:
1. Установить Docker
2. склонировать репозиторий себе на компьютер
3. открыть терминал в папке с клонированным репозиторием. (можно просто в VS code)
4. ввести команду "docker-compose up -d" после чего в приложении Docker создастся и запустится новый контейнер со всем необходимым для работы. 
5. Чтобы подключиться к окружению python в контейнере необходимо установить расширение в VS Code под названием "Dev Containers"
6. После установки расширения, при запущенном контейнере в VS Code необходимо нажать F1 на клавиатуре и выбрать "Dev Containers:Attach to Running Container..." и подключиться к запущенному контейнеру. При этом появится новое окно VS Code подключенное к контейнеру.
7.Установить в новом окне VS Code расширение Python

Библиотеки используемые в нашем окружении python прописываются в файле requirements.txt с указанием версии.


Описание структуры прокета (https://dev.to/mohammad222pr/structuring-a-fastapi-project-best-practices-53l6?ysclid=mgas49eaoe934801463)
main.py: Точка входа в приложение FastAPI.
dependencies.py: Содержит общие зависимости, такие как сеансы работы с базой данных.
app/routers/ : Маршруты
app/internal/admin.py : Содержит внутренние, закрытые для общего доступа конечные точки, такие как панель администратора.
app/core/ : Содержит конфигурации и настройки безопасности
app/models/ : Определяет модели SQLAlchemy:
Схемы app/schemas/ : Модели Pydantic для проверки данных:
app/services/ : Сервисы Содержит бизнес-логику, отдельную от маршрутов API
app/db/ : для работы с БД
tests/ : Включает модульные и интеграционные тесты

Для загрузки миграции с помощью Alembic: 
в консоли внутри VScode подключенного к контейнеру, перейти в директрию "app" с помощью команды (cd app) и  использовать команду alembic upgrade head.
Также для автоматической генерации миграций использовать команду alembic revision --autogenerate (например)

localhost:8000/docs - ссылка на Swagger UI для тестирования маршрутов...